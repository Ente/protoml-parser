@help
=name:tags
=docs:
The `@tags` block defines reusable tags or labels that can be assigned to tasks or content.
Your tags should be within an own file.

Each tag is written as `=id:label`, optionally with descriptions or aliases separated by `,`.

=examples:
@tags
=0:Urgent
=important:Critical,high-priority

@help
=name:subjects
=docs:
The `@subjects` block defines numbered topics (subjects) discussed or referenced throughout the document.

Each subject has an ID and a description, and can be echoed with `@@e={id}`.

=examples:
@subjects
=0:Project Overview
=1:Security Review
@@e=1
